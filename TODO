TODO:
	Add F-Keys and ESC to MOS (Shift-ESC doesn't exit emulator)
	EmScriptEn get it working ?
	CALL / ENDPROC (and scanner)
	Input / max length / editable.
	INPUT
	LIST
	INKEY$
	EVENT/TIME (when timer available)
	LOAD/SAVE
	MOS Redirection.

Changes:
	Input code.
