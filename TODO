TODO:
	Shift-Tokens, write tokenising/detokenising and execution code.
	Scanforward needs fixing for [[SHIFT]] ?
	Complete Code Reading (down to 56 files).

Fixes:
	Assembler file system.
	Definitions in command line bug ?

Outstanding (Implementation specific):
	EVENT/TIME (when timer available)
	LOAD/SAVE (when flash r/w available)
	Graphics stuff ?
	Assembler stuff ?
