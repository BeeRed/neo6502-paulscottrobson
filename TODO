TODO:
	Optimised 1-count FOR.	
	CALL / ENDPROC (and scanner)
	SYS
	Input / max length.
	INPUT
	LIST
	POKE/DOKE/PEEK/DEEK
	INKEY$
	EVENT/TIME (when timer available)
	LOAD/SAVE
	MOS Redirection.

Changes:
	Memory checking on string/array allocate stuff.
	Input code.
