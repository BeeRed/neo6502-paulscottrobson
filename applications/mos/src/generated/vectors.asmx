;
;	This file is automatically generated.
;
	* = $ffd0

	jmp		OSReadDirectory          ; Read file directory.
	jmp		OSDeleteFile             ; Delete file
	jmp		OSReadFile               ; Read file into memory
	jmp		OSGetScreenPosition      ; Screen position to XY
	jmp		OSGetScreenSize          ; Get size of screen to XY
	jmp		OSWriteString            ; Write length prefixed string YX to screen
	jmp		OSEnterLine              ; Edit line, return line in YX length prefixed, backspace only editing.
	jmp		OSScreenLine             ; Edit line, return line in YX length prefixed, full screen editing.
	jmp		OSKeyboardDataProcess    ; Keyboard update process.
	jmp		OSCheckBreak             ; NZ if ESC pressed.
	jmp		OSIsKeyAvailable         ; Check if key available (CS if so)
	jmp		OSReadKeystroke          ; Read A from keyboard, display cursor, wait for key.
	jmp		OSReadKeyboard           ; Read A from keyboard, CC = success, CS = no key
	jmp		OSWriteScreen            ; Write A to screen, CC = success
