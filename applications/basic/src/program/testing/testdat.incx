Line0:
	.text '96',0
Line1:
	.text '131  READ TIME PEEK( &PROC MOD DIV UNTIL DATA( OR +MOD FRAC(>>STEP SQR(PROC LEN(INKEY$( )THEN UNTIL,WEND MOD AND CHR$(',0
Line2:
	.text '89 LEFT$(SGN(PROC <<+ASC(',0
Line3:
	.text '96 ]DO WHILE - OR DO)-PEEK(TIME<<DIV<>DEEK( LEFT$(',0
Line4:
	.text '103 CALL REPEAT/ MID$( PROC LEFT$(POKE DEEK( )POKE FOR ABS(<=PEEK(OR FRAC(READ SYS UNTIL UNTIL -<>TIME XOR LOOP CALL< RIGHT$(VAL( LEFT$(',0
Line5:
	.text '117 : THEN- &=',0
Line6:
	.text '75 <<INKEY$(',0
Line7:
	.text '96 SYS <>DEC(TO LET /ENDIF THEN ENDIF DOKE VAL(MID$(OR WEND STEP AND LOOP MID$( <= >>RIGHT$(WHILE ENDIF < <WEND ]',0
Line8:
	.text '138  <>LEFT$(ABS(INKEY$(MOD FRAC(DOKE DEEK(ABS(-STR$(+ ) READ NEXT PROC SQR(SGN(>> TIME REPEAT OR=THEN ABS( INPUT',0
Line9:
	.text '75  CALL RND(DO RAND( IF<=]CHR$( <<<>LEFT$( ASC( FRAC(',0
