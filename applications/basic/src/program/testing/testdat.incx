Line0:
	.text '280  LEFT$(IF,IF MID$(INPUT FOR DIV AND LOOP PRINT WHILE&-,IF FOR CALL PRINT INPUT +POKE INKEY$(,',0
Line1:
	.text '295  STR$( SQR( #<>: ,>>>>ASC( POKE ]STEP INT( INT(CALL REPEAT RAND(DEEK( RAND(ELSE',0
Line2:
	.text '250 DEC(= XOR ASC(>=REPEAT :PEEK(MOD STEP TO ENDPROC PROC SGN(TIME XOR SQR( PEEK(THEN ABS(',0
Line3:
	.text '250  XOR VAL(THEN IF EVENT(DO WEND MOD;AND DOKE SGN(LET ELSE',0
Line4:
	.text '205 DEEK(XOR ASC( <>LOOP CALL>=>PROC LOOP READ DIV+LOOP UNTIL]',0
Line5:
	.text '235 >> REPEAT POKE;EXIT ABS( TO CHR$(OR RND( TO DOKE)PROC-DO INT( RAND(FOR] EVENT(INT( OR </XOR XOR MID$(<>DO',0
Line6:
	.text '280 ,VAL( READ LOOP WEND FOR DEEK( DEEK(POKE DOKE WEND IF UNTIL & =READ SYS EXIT= PRINT REPEAT OR EVENT(',0
Line7:
	.text '190 DO]>UNTIL)PEEK(LOOP DATA>=WHILE ABS( <> ENDIF INPUT < THEN LEN( DOKE PRINT',0
Line8:
	.text '295 OR>>DEEK(WHILE FRAC(RIGHT$(NEXT',0
Line9:
	.text '190  SGN( *PROC VAL( THEN SYS* <<',0
